#include <bits/stdc++.h>
using namespace std;

int main()
{
    vector<int> nums={1, 4, 3};
    //cout << "Hello Vscode" << endl;
    reverse(nums.begin(), nums.end());
    sort(nums.begin(), nums.end());
    return 0;
}